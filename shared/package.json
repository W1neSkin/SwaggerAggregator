{
  "name": "@swagger-aggregator/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types and API client for web and mobile",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "axios": "^1.7.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
